{
  "AccountTag": "your-cloudflare-account-id",
  "TunnelSecret": "your-tunnel-secret-key-base64-encoded",
  "TunnelID": "12345678-1234-1234-1234-123456789abc",
  "TunnelName": "sso-poc-tunnel"
}

# This is an example file showing the structure of tunnel credentials.
# The actual tunnel-credentials.json file will be generated when you create your tunnel:
#
# 1. Create the tunnel:
#    cloudflared tunnel create sso-poc-tunnel
#
# 2. Copy the generated credentials file:
#    cp ~/.cloudflared/{TUNNEL_ID}.json ./cloudflare/tunnel-credentials.json
#
# 3. Update the tunnel name in cloudflare/config.yml if different
#
# Note: Keep this file secure and do not commit it to version control!
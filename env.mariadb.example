# =============================================================================
# MariaDB Database Environment Configuration - EXAMPLE
# =============================================================================
# Copy this file to .env.mariadb and customize the values below
# These settings configure the MariaDB Docker container

# =============================================================================
# MARIADB CONFIGURATION
# =============================================================================
MYSQL_ROOT_PASSWORD=change_this_root_password               # Root password for MariaDB
MYSQL_DATABASE=sso_main                                     # Main database name
MYSQL_USER=sso_user                                         # Database user for applications
MYSQL_PASSWORD=change_this_password                         # Password for database user (must match apps)

# =============================================================================
# CONTAINER CONFIGURATION
# =============================================================================
MARIADB_VERSION=10.9                                       # MariaDB version to use

# =============================================================================
# SECURITY NOTES
# =============================================================================
# - Use strong passwords for MYSQL_ROOT_PASSWORD and MYSQL_PASSWORD
# - The MYSQL_PASSWORD must match DB_PASSWORD in all application .env files
# - In production, consider using Docker secrets instead of environment variables
# - Ensure the MariaDB container is not exposed to external networks in production